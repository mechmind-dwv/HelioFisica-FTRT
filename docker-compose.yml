# docker-compose.yml
version: '3.8'
services:
  ftrt-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:///ftrt_data.db
    volumes:
      - ./data:/app/data
